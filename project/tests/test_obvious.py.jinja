"""
Silly placeholder test for the template.
"""

from __future__ import annotations

from {{project_slug}}.{{project_slug}} import hello


def test_silly_placeholder() -> None:
    """
    Test for silly placeholder in
    {{project_slug}}.py.
    """
    want = "Hello {{project_slug}}"
    got = hello()

    assert got == want
